/**
 * PipelineHQ — AI Prompts
 * Все промпты для LLM-вызовов в одном месте
 */

export const PROMPTS = {
  /**
   * Анализ профиля Threads — определение ниши, оффера, ЦА, CTA
   */
  analyzeProfile: `Ты — эксперт по маркетинговой стратегии в социальных сетях.
Проанализируй контент пользователя из Threads и определи:

1. **Ниша** — в какой области работает этот человек
2. **Оффер** — что он продаёт или может продавать
3. **Средний чек** — предполагаемая цена его услуг/продуктов (в долларах)
4. **Целевой клиент** — кто его идеальный клиент
5. **CTA** — какой призыв к действию лучше всего подходит: "dm" (написать в DM), "call" (запись на созвон), "link" (переход по ссылке)

Ответь строго в JSON-формате.`,

  /**
   * Генерация черновика по углу
   */
  generateDraft: (
    angle: string,
    niche: string,
    offer: string,
    targetClient: string
  ) =>
    `Ты — копирайтер-эксперт для Threads. Напиши пост по шаблону "${angle}".

Контекст:
- Ниша: ${niche}
- Оффер: ${offer}
- Целевой клиент: ${targetClient}

Правила:
- Максимум 500 символов
- Текстовый формат (без эмодзи в начале строк)
- Один чёткий посыл
- Заканчивай призывом к действию или вопросом
- Пиши от первого лица
- Язык: русский`,

  /**
   * Генерация conversion action
   */
  generateConversionAction: (cta: string, niche: string) =>
    `Предложи 1 конкретное действие для генерации лида в Threads.
CTA-стратегия: ${cta}
Ниша: ${niche}
Ответ: одно предложение, конкретное и actionable.`,
} as const;
